meta {
  name: List Surveys
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/admin/surveys
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ROOT_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
