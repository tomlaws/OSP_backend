meta {
  name: Delete Survey
  type: http
  seq: 4
}

delete {
  url: {{BASE_URL}}/api/admin/surveys/697ec1a6de0bb02bc21449c6
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ROOT_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
