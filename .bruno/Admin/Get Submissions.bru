meta {
  name: Get Submissions
  type: http
  seq: 5
}

get {
  url: {{BASE_URL}}/api/admin/submissions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ROOT_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
